@model PositionViewModel

@{
  ViewData["Title"] = "D2CFL.Admin";
}

<h2>Are you realy want to delete @Model.Name</h2>

<form asp-action="Delete" asp-controller="Position">
  <div class="btn-group btn-group-justified">
    <div class="btn-group">
      <button asp-action="Delete" asp-controller="Position" type="submit" class="btn btn-outline-dark">Delete</button>
    </div>
    <a asp-action="Info/@Model.Id" asp-controller="Position" class="btn btn-outline-dark">Back</a>
  </div>
</form>