@model PositionViewModel

@{
  ViewData["Title"] = "D2CFL.Admin";
}

<h2>@Model.Name</h2>

<div class="container">
  <dl class="dl-horizontal">
    <dt>Name</dt>
    <dd>@Model.Name</dd>
    <dt>Kill Coefficient</dt>
    <dd>@Model.KillCoefficient</dd>
    <dt>Death Coefficient</dt>
    <dd>@Model.DeathCoefficient</dd>
    <dt>Assist Coefficient</dt>
    <dd>@Model.AssistCoefficient</dd>
  </dl>
  <div class="btn-group btn-group-justified">
    <a asp-action="Update" asp-controller="Position" asp-route-id="@Model.Id" class="btn btn-default">Update</a>
    <a asp-action="Delete" asp-controller="Position" asp-route-id="@Model.Id" class="btn btn-default">Delete</a>
    <a asp-action="Index" asp-controller="Position" class="btn btn-default">Back</a>
  </div>
</div>